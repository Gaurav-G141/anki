# Testing-only: paste an OpenAI API key here (one line) and rename to pgre_ai_key.txt,
# or run `just bake-ai-key` to copy it from your .env. This file is git-ignored.
# WARNING: the key is bundled in plaintext inside the built app — use a low-quota key.
