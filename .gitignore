__pycache__
.mypy_cache
.DS_Store
anki.prof
target
/user.bazelrc
.dmypy.json
/.idea/
/.vscode
/.bazel
/windows.bazelrc
/out
node_modules
.n2_db
.ninja_log
.ninja_deps
/extra
yarn-error.log
ts/.svelte-kit
.yarn
.claude/settings.local.json
.claude/user.md
.venv
docs/_build/
docs/autoapi/
.complexipy_cache
# Copyrighted / third-party source material — keep local, never publish.
*.pdf
# Speedrun: local scratch decks (research/derived; not part of the shipped app).
/anki-decks/
/categorized decks/
# Speedrun: iOS xcframework is a build artifact (regenerate via mobile/build-xcframework.sh)
mobile/AnkiCore.xcframework/
# Speedrun: compiled installers (DMG / iOS .app) are large build outputs, not source
/installers/
# Speedrun: transient SQLite WAL sidecars (never commit next to a bundled deck)
*.anki2-wal
*.anki2-shm
